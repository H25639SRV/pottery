# Cấu hình build
[build]
  # Lệnh build chính thức của React (npm run build)
  command = "npm run build"
  # Thư mục mà React scripts tạo ra sau khi build
  publish = "build" 

# Cấu hình Redirects cho SPA (Khắc phục lỗi 404)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200