services:
  # --------------------
  # 1. DATABASE SERVICE
  # --------------------
  db:
    image: postgres:14-alpine
    container_name: pottery-db-1
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: pottery
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5
    ports:
      - "5432:5432" # Vẫn giữ để truy cập DB từ host nếu cần
  
  # --------------------
  # 2. BACKEND SERVICE (Node.js/Express API)
  # --------------------
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: pottery-backend-1
    restart: always
    env_file:
      - backend/.env
    depends_on:
      db:
        condition: service_healthy
    # 🚨 SỬA: XÓA mapping cổng 5000 ra Host. Nginx sẽ truy cập nội bộ qua mạng Docker.
    ports:
    #   - "5000:5000" 
    #   - "5555:5555" # Giữ lại nếu bạn cần truy cập Prisma Studio từ Host
      - "5555:5555"
      
  # --------------------
  # 3. FRONTEND SERVICE (React/Nginx)
  # --------------------
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # 📢 SỬA: Dùng đường dẫn tương đối /api và tên miền mới
        - REACT_APP_API_URL=/api 
        - REACT_APP_CLIENT_URL=http://mocgom.io.vn
        - REACT_APP_CHAT_API_URL=/
    container_name: pottery-frontend-1
    restart: always
    environment:
      # 📢 SỬA: Dùng đường dẫn tương đối /api và tên miền mới
      - REACT_APP_API_URL=/api
      - REACT_APP_CLIENT_URL=http://mocgom.io.vn
      - REACT_APP_CHAT_API_URL=/
    depends_on:
      - backend
    # 🚨 SỬA: Map cổng 80 của container ra cổng 80 của Host
    ports:
      - "80:80" 

volumes:
  postgres_data: